/*
 * Isotipo compacto para la navegacion del admin (admin.components.graphics.Icon).
 */
export function BrandIcon() {
  return (
    <span
      aria-label="Salud Intercultural"
      style={{
        alignItems: "center",
        background: "#068ca8",
        borderRadius: "7px",
        color: "#ffffff",
        display: "flex",
        fontSize: "0.7rem",
        fontWeight: 700,
        height: "1.5rem",
        justifyContent: "center",
        width: "1.5rem"
      }}
    >
      SI
    </span>
  );
}
